name: thinkspace_analytics
version: "1.0.0"
config-version: 2

profile: thinkspace_analytics

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  thinkspace_analytics:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    silver:
      +materialized: view
    marts:
      +materialized: view
